<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Valentine Prom</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              sans: ["Inter", "system-ui", "sans-serif"]
            },
            colors: {
              void: "#050505",
              charcoal: "#121212",
              neon: "#ff0055",
              electric: "#7000ff"
            }
          }
        }
      };
    </script>
  </head>
  <body class="bg-void text-[#f4f2f5] font-sans min-h-screen relative">
    <div class="bg-mesh" aria-hidden="true">
      <span class="mesh-blob blob-1"></span>
      <span class="mesh-blob blob-2"></span>
      <span class="mesh-blob blob-3"></span>
    </div>
    <header class="sticky top-0 z-10 border-b border-white/10 bg-black/40 backdrop-blur-xl">
      <div class="mx-auto flex w-full max-w-6xl items-center justify-between px-6 py-4">
        <div class="text-sm font-semibold uppercase tracking-[0.2em] text-white/80">Prom Protocol</div>
        <% if (currentUser) { %>
        <div class="flex items-center gap-3">
          <% if (currentUser.avatar) { %>
          <img
            src="<%= currentUser.avatar %>"
            alt="avatar"
            class="h-9 w-9 rounded-full border border-white/20"
          />
          <% } %>
          <div class="hidden sm:block text-xs text-white/70">
            <div class="font-semibold text-white"><%= currentUser.name %></div>
            <div><%= currentUser.email %></div>
          </div>
          <a
            href="/auth/logout"
            class="rounded-full border border-white/15 bg-white/5 px-4 py-2 text-xs font-semibold text-white/80 transition hover:border-white/30 hover:text-white"
          >
            Logout
          </a>
        </div>
        <% } %>
      </div>
    </header>
    <main class="relative z-10 mx-auto w-full max-w-6xl px-6 py-10 lg:py-14">

