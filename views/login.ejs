<%- include("partials/header") %>

<section class="relative z-10 flex min-h-[70vh] items-center justify-center">
  <div class="w-full max-w-3xl rounded-[28px] border border-white/10 bg-white/5 p-8 shadow-[0_20px_80px_rgba(0,0,0,0.55)] backdrop-blur-2xl sm:p-12">
    <div class="text-xs uppercase tracking-[0.35em] text-white/60">The Prom Protocol</div>
    <h1 class="mt-4 text-4xl font-semibold text-white sm:text-5xl">
      Ask them to prom, anonymously.
    </h1>
    <p class="mt-4 text-base text-white/70">
      A mysterious, premium space for secret invites, honest feedback, and a softer way to say yes.
    </p>

    <div class="mt-8 flex flex-wrap items-center gap-4">
      <a
        href="/auth/google"
        class="rounded-full bg-gradient-to-r from-[#ff0055] to-[#7000ff] px-8 py-3 text-sm font-semibold text-white shadow-[0_0_30px_rgba(255,0,85,0.35)] transition hover:shadow-[0_0_40px_rgba(255,0,85,0.55)]"
      >
        Enter with Institute ID
      </a>
      <span class="text-xs text-white/60">Only institute emails are allowed.</span>
    </div>

    <div class="mt-8 grid gap-4 sm:grid-cols-3">
      <div class="rounded-2xl border border-white/10 bg-black/40 p-4">
        <p class="text-sm font-semibold text-white">Anonymous by design</p>
        <p class="mt-1 text-xs text-white/60">No identities until both sides opt in.</p>
      </div>
      <div class="rounded-2xl border border-white/10 bg-black/40 p-4">
        <p class="text-sm font-semibold text-white">Spotlight search</p>
        <p class="mt-1 text-xs text-white/60">Find anyone by name or roll number.</p>
      </div>
      <div class="rounded-2xl border border-white/10 bg-black/40 p-4">
        <p class="text-sm font-semibold text-white">Instant updates</p>
        <p class="mt-1 text-xs text-white/60">You’ll know when they respond.</p>
      </div>
    </div>

    <div class="mt-8 flex flex-wrap gap-2 text-xs text-white/50">
      <span class="rounded-full border border-white/10 bg-white/5 px-3 py-1">Mysterious</span>
      <span class="rounded-full border border-white/10 bg-white/5 px-3 py-1">Respect-first</span>
      <span class="rounded-full border border-white/10 bg-white/5 px-3 py-1">Consent-based</span>
      <span class="rounded-full border border-white/10 bg-white/5 px-3 py-1">Screenshot-worthy</span>
    </div>
  </div>
</section>

<%- include("partials/footer") %>

